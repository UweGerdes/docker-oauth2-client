/**
 * ## Model for login
 *
 * @module login/model
 */

'use strict';

let data = { modelData: 'login data' };

module.exports = {
  /**
   * get data
   *
   * @returns {object} model data
   */
  getData: () => {
    return data;
  }
};
