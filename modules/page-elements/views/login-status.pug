//- login-status.pug
if session
  #login-status.login-status
    if session.userdata
      h2= 'Angemeldet über ' + session.oauthProvider + ' als '  + session.userdata.name
    else
      h2
        a(href='/login/') anmelden
